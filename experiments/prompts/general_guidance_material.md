Render the validated blueprint into independent General Guidance.

Use only content units present in the blueprint. Do not discover, introduce, evaluate, or reject new algorithmic directions. Do not narrate your thought process. Do not continue searching after the blueprint has selected the approaches. Do not include code or pseudocode. Expand the blueprint into a complete teaching document and then stop.

Write between {{lower_bound}} and {{upper_bound}} completion tokens, targeting {{target_tokens}}. Soft section token budgets are {{section_budget}}. Structural paragraph budgets are {{paragraph_budget}}. The output-capacity limit is not a target. Stop after all blueprint items have been rendered.

Cover four substantive semantic regions in order: Constraints; Approaches; Correctness and Edge Cases; Implementation Checks. The preferred Markdown headings are `## Constraint Analysis`, `## Algorithmic Directions`, `## Correctness and Edge Cases`, and `## Implementation Checks`, but semantic completeness matters more than exact heading text. Finish every region. Output only the teaching material.

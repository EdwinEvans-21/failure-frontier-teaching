The previous material render exhausted the output capacity and was unusable.

Do not perform new reasoning or algorithm search. Use only the validated blueprint. Produce exactly four compact semantic sections, exactly once and in this order:

## Constraints
## Approaches
## Correctness
## Implementation

Use at most three bullets per section, two sentences per bullet, and two algorithmic directions total. Every section must contain substantive content. Do not narrate thought process, add an approach, or include code or pseudocode. Finish all four sections before the fixed output-capacity ceiling.

There is no required token target, lower bound, upper bound, or length-matching condition.

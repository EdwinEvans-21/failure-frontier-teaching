The previous General Guidance material was complete and semantically valid, but it was shorter than the registered token interval.

Expand the supplied material in place. Preserve every substantive claim, all existing approaches, implementation coverage, and all four semantic categories. Expand section by section instead of reorganizing or replacing the material.

You may add deeper explanations of existing constraints; state definitions, invariants, transitions, or update logic within an existing direction; the role of an already named data structure; correctness obligations; boundary and counterexample checks; initialization, indexing, complexity risks, implementation checks, and common mistakes; and comparison between the existing approaches.

Do not introduce a new algorithmic direction, a third approach, a new core reduction, a new solution, a new example, code, pseudocode, repetition, filler, meta-commentary, or thought-process narration. Do not restart the problem-solving process or replace the selected approaches. Expand vertically within the validated blueprint and existing material; do not expand horizontally into new algorithm search.

Previous completion tokens: {{observed_tokens}}
Target completion tokens: {{target_tokens}}
Accepted interval: {{lower_bound}}-{{upper_bound}}
Requested expansion scale: {{requested_scale}}

Target section budgets: {{section_budget}}
Structural paragraph budgets: {{paragraph_budget}}

{{revision_feedback}}

Missing-category feedback: {{missing_categories_feedback}}

Expand each section toward its target budget, preserve substantive coverage of constraints, approaches, correctness and edge cases, and implementation checks, finish every section, and stop naturally. Output only the expanded guidance.

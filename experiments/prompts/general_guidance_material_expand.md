The previous complete material was too short at {{previous_candidate_tokens}} completion tokens.

Rerender from the validated blueprint using scaled section budgets {{section_budget}} (scale {{budget_scale}}) and paragraph budgets {{paragraph_budget}}. Write between {{lower_bound}} and {{upper_bound}} completion tokens, targeting {{target_tokens}}.

Use only blueprint content units. Add detail only by expressing those units more fully; do not introduce a new fact, algorithmic direction, example, edge case, implementation idea, code, pseudocode, repetition, filler, or thought-process narration. Cover all four semantic regions and finish the final one. The output-capacity limit is not a target. Output only the teaching material.

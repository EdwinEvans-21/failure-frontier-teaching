The previous General Guidance candidate was unusable because it failed the semantic or content-safety requirements described below.

{{revision_feedback}}

Render the same validated blueprint again. Preserve its selected content units and at most two algorithmic directions. Do not add a new idea, example, algorithm, code, or pseudocode.

Produce all four substantive semantic regions in order: Constraints; Approaches; Correctness and Edge Cases; Implementation Checks. Every region must contain complete substantive content. Finish with `finish_reason=stop` before the fixed output-capacity ceiling.

There is no required token target, lower bound, upper bound, or length-matching condition.
